name: lbSelection
desc: Returns [[Array]] of selected rows indices in the given listbox. If listbox is of style LB_MULTI (multi-selection) the array will contain multiple elements, otherwise only a single element. For single selection listbox one can also use [[lbCurSel]]. See also [[lbIsSelected]], [[lbSetSelected]]
groups:
- GUI Control - ListBox
syntax:
- call: !Unary control
  ret:
    type: !ArrayUnsized
      value: Number
  right: !Item
    name: control
    type: Control
argument_loc: Unspecified
effect_loc: Unspecified
since: {}
examples:
- <sqf>_indices = lbSelection _control;</sqf>
see_also:
- lbAdd
- lbClear
- lbColor
- lbCurSel
- lbData
- lbDelete
- lbIsSelected
- lbPicture
- lbSetColor
- lbSetCurSel
- lbSetData
- lbSetPicture
- lbSetSelected
- lbSetTooltip
- lbSetValue
- lbSize
- lbSort
- lbSortByValue
- lbText
- lbValue
