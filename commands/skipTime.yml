name: skipTime
desc: Jumps the specified number of hours forward or backward.<br>The time of day and tides are adjusted, but no changes are made to any units. If present, the lower level of clouds instantly jump to the position they would be in if time had passed normally.
multiplayer_note: |-
  In {{arma3}} (around v1.14) [[skipTime]] executed on the server will get synced in 5 seconds or so with all the clients. It will also be [[Multiplayer Scripting#Join In Progress|JIP]] compatible. [[skipTime]] executed on a client will change time on client for about 5 seconds after which it will sync back to server time.<br>
  For other games' behaviour, see {{Link|Multiplayer Scripting#Join In Progress}}.

  {{Feature|important|Since {{GVI|arma3|2.10}} [[skipTime]] is limited to max +/- 1000 hours per skip for game stability. The command is intended for short time jumps. For large intervals, use [[setDate]].}}
groups:
- Time
- Environment
syntax:
- call: !Unary duration
  ret:
    type: Nothing
  right: !Item
    name: duration
    desc: hours to skip. A positive value will create a forward time jump, a negative value will jump backwards.
    type: Number
argument_loc: Unspecified
effect_loc: Unspecified
server_exec: true
since: {}
examples:
- <sqf>skipTime 5;</sqf>
- <sqf>skipTime (5/60); // be sure to use parentheses, otherwise here (skipTime 5)/60 will happen</sqf>
- "<sqf>\nwhile {true} do \n{\n\tskipTime 0.00333;\n\tsleep 0.1; // smooth time transition\n};\n</sqf>"
- <sqf>skipTime ((_timeToSkipTo - dayTime + 24) % 24); // skip forward to a specific time, irrespective of the current mission time</sqf>
see_also:
- setDate
- accTime
- setTimeMultiplier
- timeMultiplier
- BIS_fnc_setDate
