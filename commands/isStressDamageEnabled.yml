name: isStressDamageEnabled
desc: |-
  Returns [[true]] if stress damage is enabled in the game settings. A getter for [[enableStressDamage]].
  {{Feature|afm}}
groups:
- RTD
syntax:
- call: Nular
  ret:
    type: Boolean
    desc: Returns true if enabled, otherwise false
argument_loc: Local
effect_loc: Local
since:
  arma3: '1.34'
examples:
- <sqf>_settingEnabled = isStressDamageEnabled;</sqf>
see_also:
- enableStressDamage
