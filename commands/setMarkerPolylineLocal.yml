name: setMarkerPolylineLocal
desc: Sets a polyline marker's path. Works only on the computer that executes this command.
groups:
- Markers
syntax:
- call: !Binary
  - marker
  - path
  ret:
    type: Nothing
  left: !Item
    name: marker
    type: String
  right: !Item
    name: path
    desc: '{{hl|[x1, y1, x2, y2, ... xn, yn]}}'
    type: !ArrayUnsized
      value: Number
argument_loc: Unspecified
effect_loc: Local
since:
  arma3: '2.02'
examples:
- "Changes {{hl|\"marker_1\"}} into a polyline and draws a sine curve on your position:\n<sqf>\n\"marker_1\" setMarkerShape \"polyline\";\nprivate _return = [] ;\n\nfor \"_i\" from 0 to 100 step 0.1 do\n{\n\t_return pushBack (_i + getPos player#0);\n\t_return pushBack ((sin (_i*10))*10 + getPos player#1);\n} ;\n\n\"marker_1\" setMarkerPolylineLocal _return;\n</sqf>"
see_also:
- markerPolyline
- setMarkerPolyline
